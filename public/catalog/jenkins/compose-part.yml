image: jenkins/jenkins:lts
restart: unless-stopped
volumes:
  - ${JENKINS_DATA:-./data/jenkins}:/var/jenkins_home
ports:
  - "8080:8080"
  - "50000:50000"
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8080"]
  interval: 30s
  timeout: 10s
  retries: 3