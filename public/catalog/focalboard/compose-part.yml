image: mattermost/focalboard:edge
restart: unless-stopped
volumes:
  - ${FOCALBOARD_DATA:-./data/focalboard}:/opt/focalboard/data
environment:
  - VIRTUAL_PORT=8000
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8000"]
  interval: 30s
  timeout: 10s
  retries: 3