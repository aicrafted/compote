image: frappe/erpnext:v16.12.0
restart: unless-stopped
volumes:
  - ${ERPNEXT_DATA:-./data/erpnext}:/home/frappe/frappe-bench/sites
environment:
  - DB_PASSWORD=${DB_PASSWORD}
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:8080"]
  interval: 30s
  timeout: 10s
  retries: 3