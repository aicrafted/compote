image: sonatype/nexus3:3.91.0
restart: unless-stopped
volumes:
  - ${NEXUS_DATA:-./data/nexus}:/nexus-data
healthcheck:
  test: ["CMD", "wget", "-qO-", "http://localhost:8081/"]
  interval: 30s
  timeout: 10s
  retries: 3