image: mumblevoip/mumble-server:v1.6.0
restart: unless-stopped
volumes:
  - ${MUMBLE_DATA:-./data/mumble}:/data
ports:
  - "64738:64738"
  - "64738:64738/udp"
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost:64738"]
  interval: 30s
  timeout: 10s
  retries: 3