image: jitsi/web:stable
restart: unless-stopped
volumes:
  - ${JITSI_CONFIG:-./data/jitsi}:/config
ports:
  - "8000:80"
  - "8443:443"
healthcheck:
  test: ["CMD", "curl", "-f", "http://localhost"]
  interval: 30s
  timeout: 10s
  retries: 3