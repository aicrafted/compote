image: mvance/unbound:latest
restart: unless-stopped
volumes:
  - ${UNBOUND_CONFIG:-./config/unbound}:/etc/unbound
ports:
  - "53:53/tcp"
  - "53:53/udp"
healthcheck:
  test: ["CMD", "nslookup", "google.com", "localhost"]
  interval: 30s
  timeout: 10s
  retries: 3